# Moved

This write-up now lives with the runner:

**[experiments/mnist_matched_size/PROTOCOL.md](../experiments/mnist_matched_size/PROTOCOL.md)**

```bash
uv run --extra train python experiments/mnist_matched_size/train.py --scale
```
